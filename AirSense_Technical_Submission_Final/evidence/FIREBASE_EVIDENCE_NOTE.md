# Firebase evidence

Firebase console screenshots were supplied showing:
- `/readings` containing multiple historical push-key records
- `/sensor` containing the latest humidity, PM2.5, status, temperature and timestamp

The exact raw database export is included under `data/raw/` and is the primary numerical source for this submission.
